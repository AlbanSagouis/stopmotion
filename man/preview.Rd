% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{preview}
\alias{preview}
\title{Preview an animation}
\usage{
preview(images, fps = 10, loop = 0, frames = NULL, label = TRUE)
}
\arguments{
\item{images}{an object of class \code{magick-image} to modify}

\item{fps}{playback speed in frames per second.  Must be a positive integer
divisor of 100, because GIF delay is stored in hundredths of a second
(\code{delay = 100 / fps}).  Valid values: 1, 2, 4, 5, 10, 20, 25, 50,
100.  Defaults to \code{10}.}

\item{loop}{a non-negative integer giving the number of times the animation
loops.  \code{0} (the default) means loop forever.}

\item{frames}{integer vector of frame indices to duplicate. Defaults to
\code{NULL}, which duplicates all frames.}

\item{label}{logical. Whether to overlay the frame index and label on each
frame.  Defaults to \code{TRUE}.}
}
\value{
a \code{magick-image} object containing the animated sequence.
}
\description{
Converts a stack of frames into an animated GIF for display, with each
frame's index and label overlaid as text.  In an interactive session the
animation opens in the system viewer; in a knitr/Quarto document it is
embedded as an inline animated GIF.  Wraps \code{magick::image_animate}.
}
\examples{
\dontrun{
  dino_dir <- system.file("extdata", package = "stopmotion")
  images <- read(dir = dino_dir)
  preview(images)
  preview(images, fps = 5)
  preview(images, label = FALSE)
}
}
