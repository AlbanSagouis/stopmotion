% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop.R
\name{crop}
\alias{crop}
\title{Crop images}
\usage{
crop(images, geometry, gravity = NULL, repage = TRUE, frames = NULL)
}
\arguments{
\item{images}{an object of class \code{magick-image} to modify}

\item{geometry}{a geometry string specifying the cropped region,
e.g. \code{"100x100+10+10"} (widthxheight+x_offset+y_offset).}

\item{gravity}{anchor point for the crop: one of \code{"NorthWest"},
\code{"North"}, \code{"NorthEast"}, \code{"West"}, \code{"Center"},
\code{"East"}, \code{"SouthWest"}, \code{"South"}, \code{"SouthEast"}.
Defaults to \code{NULL} (top-left).}

\item{repage}{logical. If \code{TRUE} (default), resets the virtual canvas
after cropping.}

\item{frames}{integer vector of frame indices to duplicate. Defaults to
\code{NULL}, which duplicates all frames.}
}
\value{
a \code{magick-image} object
}
\description{
Crops selected frames to a given geometry. Wraps \code{magick::image_crop}.
}
\section{Verbosity}{

  After each operation a message listing the updated frame sequence is
  printed in interactive sessions.  Use
  \code{stopmotion_verbosity(FALSE)} to suppress these messages, or set
  \code{options(stopmotion.verbose = FALSE)} in your script or
  \file{.Rprofile}.
}

\examples{
\donttest{
  dino_dir <- system.file("extdata", package = "stopmotion")
  images <- read(dir = dino_dir)
  crop(images = images, geometry = "200x200+50+50")
  crop(images = images, geometry = "200x200", gravity = "Center", frames = 1:3)
}
}
